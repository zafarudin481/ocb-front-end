// event listener to initiate the quiz

const initiateButton = document.getElementById("initiate-button");

const eventToAlert = initiateButton.addEventListener("click", function () {
    alert("butang mula sudah ditekan");
})